# apps

**Purpose:** Member-facing and Operator-facing applications.
**Owner:** _unassigned_
**Nia OS books:** Book III (design), VI (components), VII (screens), V §2.5 (clients).
**Local setup:** `member` and `operator` are Flutter (pub); `console` is web (pnpm).
**Testing:** End-to-end and synthetic-Member tests for every Member-visible flow.
