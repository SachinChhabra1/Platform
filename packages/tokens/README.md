# packages/tokens

**Purpose:** The single theme module (Book VI) — colour, type scale, spacing, radius,
shadow, motion timing. The one source of truth for both design systems. The four
reserved colours (navy, green, amber, red) carry meaning only.
**Owner:** _unassigned_
**Nia OS books:** Book VI (theme centralisation), III (visual rules).
**Local setup:** _TBD — published to Flutter and web design systems._
**Testing:** Token contract tests; the `no-hardcoded-theme` gate enforces single source.
