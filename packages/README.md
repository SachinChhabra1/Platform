# packages

**Purpose:** Shared libraries — design tokens, design systems, types, i18n, test utils.
**Owner:** _unassigned_
**Nia OS books:** Book VI (components/tokens), VIII (types), III (design), V (testing).
**Local setup:** Mixed: TypeScript (pnpm) and Flutter (pub) members — see each package.
**Testing:** Per package; components are floor-tested (lowest-spec phone, dim light).
