# packages/design-system-flutter

**Purpose:** The Flutter component library for the Member and Operator apps. One
component per concept; variants via props (Book VI). Reads from packages/tokens.
**Owner:** _unassigned_
**Nia OS books:** Book VI (all), III (design), VII (screen composition).
**Local setup:** _TBD — Dart/pub package._
**Testing:** Per-component states + accessibility + motion contracts; floor-tested.
