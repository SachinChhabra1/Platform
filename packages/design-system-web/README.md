# packages/design-system-web

**Purpose:** The web component library for the Theatre Console. Same design language as
the Flutter system, reading the same packages/tokens. Not a separate design system —
a second implementation of one (DECISIONS.md ADR-003).
**Owner:** _unassigned_
**Nia OS books:** Book VI, III, VII (screens 11.x).
**Local setup:** _TBD — pnpm workspace member._
**Testing:** Per-component states + accessibility; token parity with Flutter system.
