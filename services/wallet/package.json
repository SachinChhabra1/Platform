{
  "name": "@nia/wallet",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Wallet cluster service. This slice: the read-only Wallet Overview read model (spec 0001 §3 legibility; ADR-0008). No ledger engine, no money movement, no policy.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/start.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nia/log": "workspace:^",
    "@nia/runtime": "workspace:^",
    "fastify": "^5.9.0"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
