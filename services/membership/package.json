{
  "name": "@nia/membership",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Membership cluster service — the Member relationship and its lifecycle state machine (Prospective -> Member -> Paused -> Closed). Domain core only, no HTTP yet (spec 0001 §5, §13, §14 step 2).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "start": "tsx src/start.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nia/log": "workspace:^",
    "@nia/runtime": "workspace:^",
    "fastify": "^5.9.0"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
