{
  "name": "@nia/sessions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Session issuance surface (spec 0002, plan Slice B). Issues opaque, device-bound session tokens on phone-first re-proof, writing into the @nia/runtime SessionStore (one active device, FD-S3). No verification-strength (OTP) yet; no operator/recovery path (later slices).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nia/runtime": "workspace:^",
    "fastify": "^5.9.0"
  },
  "devDependencies": {
    "tsx": "^4.22.4",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
