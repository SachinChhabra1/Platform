# 11 · Operations

Runtime, recovery, tooling. **Today:** the `nia` CLI (`../../bin/nia`) is the operational surface —
`nia verify` (green + codegen-drift check), `nia bundle create|verify|restore`, `nia preview` (dev
preview on the composed backend). The money-movement backend is **paused** (Product Polish). No
production runtime/observability yet. 🕳 Fill this out when services run.
